import React from "react";
import { HeaderContainer } from "./styles";

const Header: React.FC = () => {
  return (
    <HeaderContainer>
      <h1>Curitiba Covid Api</h1>
    </HeaderContainer>
  );
};

export default Header;
